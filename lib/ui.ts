/** Shared class strings for the drafting sheet vocabulary. */

/** The content column: every band on the site sits on this measure. */
export const frame = 'mx-auto w-full max-w-[1240px] px-5 sm:px-8 lg:px-10';

/** Micro-label voice — codes, counts, coordinates, section indices. */
export const label =
  'text-[10.5px] uppercase tracking-[0.16em] text-fd-muted-foreground';

/** Micro-label that has been marked up into the accent. */
export const labelActive =
  'text-[10.5px] uppercase tracking-[0.16em] text-fd-primary';

/** Hairline separator between stacked bands. */
export const seam = 'border-t border-fd-border';
